线程共享区:
    堆(1.8之后包含字符串常量池，之前在方法区):存储的全部是对象(对象的成员变量也在这里)
    方法区:储存虚拟机加载的类信息class和static变量、常量、（编译后的代码）
    (永久带:是hotspot对方法区的一种实现，方法区是jvm的规范)
线程私有区:
    栈:栈帧 每个方法会创建一个栈帧，方法结束时释放（存储局部变量表，操作栈，方法返回值。局部变量表放着基本数据类型，还有对象的引用。基础数据类型的对象）
    本地方法栈:专门为native方法服务
    程序计数器:程序内存地址