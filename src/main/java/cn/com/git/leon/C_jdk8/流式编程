forEach 遍历
filter 过滤
map 对结果集做处理，将对象转化为其它对象
sorted 排序
limit:对一个Stream进行截断操作，获取其前N个元素，如果原Stream中包含的元素个数小于N，那就获取其所有的元素；
skip:返回一个丢弃原Stream的前N个元素后剩下元素组成的新Stream，如果原Stream中包含的元素个数小于N，那么返回空Stream；
collect(Collectors.XXX) 将流转化成集合或字符串Collectors.toList() Collectors.joining(", ")等
distinct去重(根据特定条件)默认==
