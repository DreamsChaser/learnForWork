超时（timeout）
重试（retry）
幂等(idempotent)
熔断（circuit break）


限流(current limiting)
1、Nginx接入层限流（网关层面）
  按照一定的规则如帐号、IP、系统调用逻辑等在Nginx层面做限流
2、（应用层面）
通过redis 每个接口都对应一个令牌桶 存放redis（key方法名 value令牌数量）
通过setnx(current,current-1)命令获取令牌 不小于0并且返回成功则执行方法  失败则抛出异常或者等待
并另起一个线程补充令牌桶的令牌数量